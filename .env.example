# ============================================================
# LARAVEL BLOG - CAU HINH MOI TRUONG
# ============================================================

# ------------------------------------------------------------
# 1. CAU HINH UNG DUNG (APP)
# ------------------------------------------------------------
APP_NAME="Laravel Blog"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_TIMEZONE=Asia/Ho_Chi_Minh
APP_URL=http://localhost:8000

# Ngon ngu mac dinh
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

# Bao tri
APP_MAINTENANCE_DRIVER=file

# Ma hoa mat khau (so vong hash - cao hon = an toan hon nhung cham hon)
BCRYPT_ROUNDS=12

# ------------------------------------------------------------
# 2. LOGGING (GHI LOG)
# ------------------------------------------------------------
LOG_CHANNEL=stack
LOG_STACK=single
LOG_LEVEL=debug

# ------------------------------------------------------------
# 3. MONGODB (DATABASE CHINH)
# ------------------------------------------------------------
# MongoDB la NoSQL database, luu du lieu dang document (JSON)
# Port mac dinh: 27017
#
# Chay MongoDB bang Docker:
#   docker run -d -p 27017:27017 --name mongodb mongo:latest
#
DB_CONNECTION=mongodb
MONGODB_URI=mongodb://localhost:27017
MONGODB_DATABASE=blog_db

# ------------------------------------------------------------
# 4. REDIS (CACHE VA SESSION)
# ------------------------------------------------------------
# Redis la gi?
# - In-memory database (luu tren RAM) => cuc nhanh
# - Dung de: Cache data, luu Session, Queue job
#
# Tai sao can Redis?
# - CACHE: Luu tam ket qua query de khong phai truy van DB lien tuc
#   Vi du: Danh sach posts duoc cache 1 gio, user truy cap se load nhanh
# - SESSION: Luu thong tin dang nhap cua user
#   Thay vi luu vao file/database (cham), Redis luu tren RAM (nhanh)
#
# Port mac dinh: 6379
#
# Chay Redis bang Docker:
#   docker run -d -p 6379:6379 --name redis redis:latest
#
# Kiem tra Redis:
#   docker exec redis redis-cli ping  (tra ve PONG la OK)
#
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379
REDIS_DB=0

# Su dung Redis cho Cache
CACHE_STORE=redis
CACHE_PREFIX=blog_cache_

# Su dung Redis cho Session
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# ------------------------------------------------------------
# 5. CAC CAU HINH KHAC
# ------------------------------------------------------------
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

# Mail (hien tai chi log, khong gui that)
MAIL_MAILER=log
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# ============================================================
# TONG KET CAC PORT:
# ============================================================
# Laravel (PHP):     http://localhost:8000
# Vite (Frontend):   http://localhost:5173  (dev server)
# MongoDB:           localhost:27017
# Redis:             localhost:6379
# ============================================================

